# ──────────────────────────────────────────────────────────────────────────────
# GCP Inventory Script — Dependências Python
# ──────────────────────────────────────────────────────────────────────────────
#
# O script usa principalmente a gcloud CLI via subprocess.
# As bibliotecas abaixo fornecem autenticação e acesso às APIs GCP
# para casos onde a CLI não está disponível ou para extensões futuras.
#
# Instalação:
#   pip install -r requirements.txt
# ──────────────────────────────────────────────────────────────────────────────

# Autenticação e cliente base do Google
google-auth>=2.29.0
google-auth-httplib2>=0.2.0
google-api-python-client>=2.126.0

# Cloud Compute Engine
google-cloud-compute>=1.19.0

# Cloud Storage
google-cloud-storage>=2.16.0

# BigQuery
google-cloud-bigquery>=3.20.0

# Cloud Functions
google-cloud-functions>=1.16.0

# Cloud Run
google-cloud-run>=0.10.0

# Cloud SQL
cloud-sql-python-connector>=1.9.0

# Pub/Sub
google-cloud-pubsub>=2.21.0

# Firestore (opcional — usado por alguns inventários)
google-cloud-firestore>=2.16.0

# Cloud Scheduler
google-cloud-scheduler>=2.13.0

# Cloud Tasks
google-cloud-tasks>=2.16.0

# Secret Manager
google-cloud-secret-manager>=2.20.0

# Artifact Registry
google-cloud-artifact-registry>=1.11.0

# IAM (Service Accounts, Roles)
google-cloud-iam>=2.14.0

# Resource Manager (informações do projeto)
google-cloud-resource-manager>=1.12.0

# Container / GKE
google-cloud-container>=2.47.0
